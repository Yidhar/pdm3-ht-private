export CROP_CACHE_DIR="${CROP_CACHE_DIR:-/workspace/PDM/data/cropped_uint8/imagenet1k_train_256_adm_safetensors}"
export EXPECTED_TRAIN_SIZE="${EXPECTED_TRAIN_SIZE:-1281167}"
export IMAGE_SIZE="${IMAGE_SIZE:-256}"

# Primary/gated BFL repo was unavailable in this environment; this diffusers repo exposes the same VAE class/files.
export FLUX2_REPO_ID="${FLUX2_REPO_ID:-diffusers/FLUX.2-dev-bnb-4bit}"
export FLUX2_SUBFOLDER="${FLUX2_SUBFOLDER:-vae}"
export FLUX2_VAE_CLASS="${FLUX2_VAE_CLASS:-AutoencoderKLFlux2}"

export FLUX2_SMOKE64_OUT="${FLUX2_SMOKE64_OUT:-/workspace/PDM/data/vae_latents/AutoencoderKLFlux2_d32/imagenet256_train_smoke64}"
export FLUX2_SMOKE8192_OUT="${FLUX2_SMOKE8192_OUT:-/workspace/PDM/data/vae_latents/AutoencoderKLFlux2_d32/imagenet256_train_smoke8192}"
export FLUX2_FULL_OUT="${FLUX2_FULL_OUT:-/workspace/PDM/data/vae_latents/AutoencoderKLFlux2_d32/imagenet256_train_full}"

export DEVICE="${DEVICE:-cuda:0}"
export MODEL_DTYPE="${MODEL_DTYPE:-fp32}"
export SAVE_DTYPE="${SAVE_DTYPE:-bf16}"
export LATENT_MODE="${LATENT_MODE:-mode}"
export BATCH_SIZE="${BATCH_SIZE:-64}"
export LATENT_SHARD_SIZE="${LATENT_SHARD_SIZE:-4096}"
